spring.application.name=ticket-service
server.port=8080

# 存储开关：memory=内置内存（默认，无需数据库）；postgres=PostgreSQL + Flyway
ticket.storage=memory

# SLA 超时升级（演示用短阈值；生产按业务 SLA 调大，如 300/3600 秒）
ticket.sla.new-seconds=30
ticket.sla.processing-seconds=60
ticket.sla.scan-ms=10000
